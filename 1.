let line=prompt("Enter numbers seperated by ,:");
let a=line.split(",").map(Number);
let b=new Array();
let c=0;
for(let i=0;i<a.length;i++){
    if(a[i]!=0)
    b.push(a[i]);
    if(a[i]==0)
    c++;
}
for(let i=0;i<c;i++)
b.push(0);
console.log(b);
